# HRAvatar — authoritative pip pin set for Python 3.11 + PyTorch 2.9.1 + CUDA 12.8.
# Mirrors MTamon/DECA release/cuda128 and MTamon/smirk release/cuda128.
# Always install with `pip install --no-deps` (see setup.sh) so that
# transitive resolution does not override these pins.

--extra-index-url https://download.pytorch.org/whl/cu128

# --------- Torch + Triton -------------------------------------------------
typing_extensions==4.15.0
torch==2.9.1
torchvision==0.24.1
triton==3.5.1

# --------- Numerical stack (numpy 2.x) -----------------------------------
numpy==2.2.6
scipy==1.16.3
scikit-image==0.25.2
scikit-learn==1.7.2
numba==0.62.1
llvmlite==0.45.1

# --------- Imaging I/O ---------------------------------------------------
opencv-python==4.12.0.88
pillow==12.0.0
ImageIO==2.37.2
imageio-ffmpeg==0.5.1
tifffile==2025.10.16
av==12.3.0
pims==0.7

# --------- 3D / face tracking --------------------------------------------
kornia==0.8.2
kornia_rs==0.1.10
fvcore==0.1.5.post20221221
iopath==0.1.10
face-alignment==1.4.1
face-detection-tflite==0.6.0

# --------- MediaPipe + JAX (pinned to the SMIRK-approved versions) -------
mediapipe==0.10.14
jax==0.4.30
jaxlib==0.4.30
ml_dtypes==0.4.1
opt_einsum==3.4.0
absl-py==2.1.0
attrs==24.2.0
flatbuffers==24.3.25
protobuf==4.25.5

# --------- Lightning / HF ------------------------------------------------
timm==0.9.16
pytorch_lightning==2.5.2
torchmetrics==1.6.0
lightning-utilities==0.11.9
transformers==4.44.2
huggingface_hub==0.25.2
safetensors==0.4.5
diffusers==0.30.3

# --------- Config / utilities -------------------------------------------
omegaconf==2.3.0
antlr4-python3-runtime==4.9.3
yacs==0.1.8
PyYAML==6.0.3
Cython==0.29.35
ninja==1.13.0
tqdm==4.67.1
loguru==0.7.2
einops==0.8.0
natsort==8.4.0
filelock==3.20.0
fsspec==2025.10.0
future==1.0.0
ipdb==0.13.13
tensorboard==2.17.1
# gdown is used by download_assets.sh to pull Google-Drive-hosted checkpoints
# (DECA deca_model.tar, SMIRK SMIRK_em1.pt, face-parsing 79999_iter.pth).
# Same 5.2.0 version pinned by MTamon/smirk@release/cuda128 install_128.sh.
gdown==5.2.0

# --------- HRAvatar-only pins (NOT in the MTamon references) ------------
# pyshtools — SH rotation helper in utils/sh_utils.py (filter_envmap path).
pyshtools==4.13.1
# plyfile — used by scene/gaussian_model.py save/load.
plyfile==1.0.3
# roma — rotation utilities in preprocess.
roma==1.5.1
# carvekit-colab — background removal for preprocess/crop_and_matting.py.
carvekit-colab==4.1.2
# torchfile — reserved for checkpoint compatibility.
torchfile==0.1.0

# --------- Build-from-source deps (HRAvatar-specific) -------------------
# pytorch3d: HRAvatar imports `pytorch3d.ops` in scene/gaussian_head_model.py
# and `pytorch3d` in utils/loss_utils.py.  No wheel exists for cu128 yet.
pytorch3d @ git+https://github.com/facebookresearch/pytorch3d.git@V0.7.8
# nvdiffrast: used by net_modules/NVDIFFREC/*.  Build from NVlabs HEAD.
nvdiffrast @ git+https://github.com/NVlabs/nvdiffrast.git
# chumpy: required for loading legacy FLAME pkls with pickle+chumpy arrays.
# The PyPI wheel breaks under numpy 2.x; use the maintained git fork.
chumpy @ git+https://github.com/mattloper/chumpy.git
# taming-transformers + CLIP — IntrinsicAnything preprocess deps.
taming-transformers-rom1504 @ git+https://github.com/CompVis/taming-transformers.git
clip @ git+https://github.com/openai/CLIP.git
